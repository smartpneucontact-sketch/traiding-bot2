# Data and caches (regenerable / machine-specific)
data_cache.pkl
__pycache__/
*.pyc
.DS_Store

# Binary artifact caches (regenerable from code + data_cache)
*.parquet
ml_track/cache/

# Norgate exported data — LICENSED, must never enter any shared/public repo
# (subscriber-only license; see SETUP_NORGATE_VM.md). Whole trees ignored,
# including manifest.json / .meta.json sidecars, not just parquets.
data_norgate_raw/
data_norgate/

# Claude Code local state (personal, not for the repo)
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/scheduled_tasks.json
