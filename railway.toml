[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Always-on web service (scheduler handles the cron internally)
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
